import { Box, Typography } from "@mui/material";

export default function Footer() {
  return (
    <Box minHeight={200} border={2} mt={3}>
      <Typography>Here goes all the contact details</Typography>
    </Box>
  );
}
